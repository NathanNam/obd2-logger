# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Runtime data root if the dev picks the repo root as their data folder
/data
/profiles

# Native iOS app build artifacts (the ios-native/ Xcode project files
# are committed, but Xcode's local build outputs / user data are not)
ios-native/build
ios-native/DerivedData
ios-native/**/xcuserdata

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
